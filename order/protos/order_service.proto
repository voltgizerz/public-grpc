syntax = "proto3";

package order;
option go_package = "./gen";

import "protos/get_order.proto";

// The order service definition.
service OrderService {
  // GetOrder
  rpc GetOrder (GetOrderRequest) returns (GetOrderResponse) {}
}
